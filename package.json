{
  "name": "@xifo/mirai-root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --scope='{@xifo/mirai-ui,@xifo/mirai-system}'",
    "build:docs": "lerna run build --scope=@xifo/mirai-docs",
    "dev": "yarn --cwd ./packages/docs dev"
  },
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "workspaces": [
    "packages/*"
  ]
}
